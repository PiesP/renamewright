import { APP_NAME, APP_TAGLINE } from './app-meta';

export function App() {
  return (
    <main>
      <h1>{APP_NAME}</h1>
      <p>{APP_TAGLINE}</p>
      <p>Read-only planning foundation.</p>
    </main>
  );
}
