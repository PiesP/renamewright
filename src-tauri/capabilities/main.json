{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "No Tauri core APIs are exposed; only registered Renamewright commands are available.",
  "windows": ["main"],
  "permissions": []
}
